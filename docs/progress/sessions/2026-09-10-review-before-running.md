# 工作会话：2026-09-10 - 手敲代码运行前复查

## 目标

将项目所有者确认的协作顺序固化，减少转写错误导致的重复运行。

## 实际完成

- 将所有手敲代码的顺序统一为：手敲完成、静态复查、必要修正与再次复查、运行验证、下一段。
- 明确助手同时审查自己给出的代码；与示例一致不等于正确，静态检查也不代替真实运行。
- 替换协作规范中“复查通过即交付下一段源码与验证命令”的旧规则，并在当前进度入口和接力稿要求中同步。
- 旧会话保留为历史事实；后续施工按更新后的现行规范执行。

## 修改的文件

- `docs/standards/collaboration.md`
- `docs/progress/current.md`
- 本会话记录。

## 验证证据

- 项目所有者提供的 `TestCatalogConstraintsIntegrationTeamUnique` 输出显示两个子测试均通过；此前三类资源字段 CHECK 的 84 个子测试也已有分段运行通过记录。上述记录不等于完整施工包或全量回归通过。
- 本次只修改协作文档，不执行测试或数据库操作；文档差异与空白检查结果见本次交接。

## 偏差、风险或待确认事项

- 约束测试仍在 `test/catalog-db-constraints` 分支施工，尚未完成全量验证、CI 或合并。

## 下一次从这里继续

- 具体文件：`internal/catalog/postgres/constraints_integration_test.go`。
- 具体任务：继续 Service、Environment 的唯一约束，以及外键和引用删除的直接 SQL 验证。
- 验收命令：每段静态复查通过后再交付对应的定向测试命令。
- 不要做：不要把静态复查或已有分段通过记录写成整个约束施工包完成。
