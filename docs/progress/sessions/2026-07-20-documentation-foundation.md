# 工作会话：2026-07-20 - 文档体系与协作规则

## 目标

建立能在新会话中恢复上下文的项目文档结构，并明确施工、学习、技术决策和交接方式。

## 实际完成

- 建立 `docs/` 根说明书、项目上下文、工程规范、进度记录、阶段施工包、架构 ADR、知识笔记地图、runbook/benchmark/postmortem 入口和模板。
- 为 M0 建立可执行的 `plan.md`、`contracts.md`、`checklist.md`、`outcome.md`。
- 建立重要技术选择的“提案 → ADR → 实施证据”流程。
- 建立协作决策规则：重要且有学习/长期影响的选择先解释概念、利弊和推荐项，再由项目所有者确认。
- 建立新会话阅读顺序，以及 `reference/` 与原始方案书的按需回读规则。
- 添加 UTF-8/LF 文本约定：`.editorconfig` 与 `.gitattributes`。

## 修改的文件

- `.editorconfig`
- `.gitattributes`
- `docs/` 下的文档体系

## 验证证据

```text
检查：列出 docs 下的全部文件，并核对文档内部路径引用。
结果：文档目录、M0 施工包、知识分类、阶段入口和模板均已存在；未发现明显路径错误。
```

## 偏差、风险或待确认事项

- 未生成 M0 业务/服务代码；这是刻意保持的边界，待下一次按 M0 施工包手写。
- Docker 宿主运行状态曾受当前执行沙箱限制，仍应在直接 SSH 会话中完成最终核验。
- 原始 `reference/` 材料不应每次完整重读，除非出现路线调整或文档上下文不足。

## 下一次从这里继续

- 具体文件：`docs/stages/m0-engineering-baseline/plan.md`
- 具体任务：按 M0 施工清单开始第一个可运行 HTTP 服务骨架。
- 验收命令：以 M0 `plan.md` 的“验收命令”为准。
- 不要做：不要提前引入数据库、容器编排、消息队列、遥测栈、前端或 AI 功能。
