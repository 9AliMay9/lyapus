# 工程规范

这些规范优先级高于单个阶段文档。需要改变规范时，先修改对应文档，并在 ADR 或会话记录中说明原因。

- `naming.md`：目录、包、类型、接口、数据库与配置命名。
- `go.md`：Go 实现、错误、context、日志和测试约定。
- `api.md`：HTTP API 和错误语义约定。
- `documentation.md`：文档、代码和证据之间的关系。
- `collaboration.md`：协作式手敲代码、临时接力稿与确认流程。
